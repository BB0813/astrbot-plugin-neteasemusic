{
  "api_url": {
    "type": "string",
    "label": "API 地址",
    "description": "Netease-CDN-Bypass 服务地址（默认：http://127.0.0.1:3000）",
    "default": "http://127.0.0.1:3000"
  },
  "quality": {
    "type": "string",
    "label": "音质",
    "description": "播放音质（lossless 无损 / exhigh 极高 / higher 高 / standard 标准）",
    "enum": ["lossless", "exhigh", "higher", "standard"],
    "default": "exhigh"
  }
}
